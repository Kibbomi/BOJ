#include <cstdio>
#include <algorithm>
#include <vector>
using namespace std;

int n, m;
vector<int> v;
int main()
{
	scanf("%d %d", &n, &m);
	v.resize(n);

	for (int i = 0; i < n; ++i)
		scanf("%d", &v[i]);
	sort(v.begin(), v.end());

	printf("%d", v[m - 1]);

	return 0;
}
