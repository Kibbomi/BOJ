#include <stdio.h>
using namespace std;

int main(void)
{
	int a, b, c;

	scanf("%d %d %d", &a, &b, &c);

	if (b >= c) {
		printf("-1");
		return 0;
	}

	int ret;
	ret = a / (c - b);

	ret++;
  
	printf("%d", ret);
  
	return 0;
}
