#include <stdio.h>
#include <algorithm>
using namespace std;

int main(void)
{
	
	int t;
	scanf("%d", &t);
	
	for(int n=1; n<=t; n++)
	{
		int a, b;
		scanf("%d %d", &a, &b);

		printf("Case #%d: %d\n",n, a + b);

	}
	
	return 0;
}
