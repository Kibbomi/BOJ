#include <stdio.h>

int main (void)
{

	int n;
	scanf("%d", &n);

	int cnt = 0;
	
	for (int i = 0; i < 5; i++)
	{
		int m;
		scanf("%d", &m);
		if (n == m)
			cnt++;
	}
	
	printf("%d", cnt);
	return 0;
}
