#include <stdio.h>


int main(void)
{
	int cache;
	int sum=0;
	for (int i = 0; i < 5; i++)
	{
		scanf("%d", &cache);

		if (cache < 40)
			cache = 40;

		sum += cache;

	}

	printf("%d", sum / 5);
	return 0;
}
