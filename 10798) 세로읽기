#include <stdio.h>

int main (void)
{
	char temp[5][16] = { 0 };

	for (int i = 0; i < 5; i++)
	{
		scanf("%s", temp[i]);
	}

	for (int j = 0; j < 16; j++)
	{
		for (int i = 0; i < 5; i++)
		{
			if(temp[i][j] != 0)
				printf("%c", temp[i][j]);
		}
	}
}
