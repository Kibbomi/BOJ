#include <stdio.h>

int main(void)
{
	

	for (int i = 0; i < 100; i++)
	{
		char input[101] = { 0 };
		char temp;
		scanf("%[^\n]s", input);
		scanf("%c", &temp);
		printf("%s\n", input);
	}
	
	
	return 0;
}
