#include <stdio.h>
#include <algorithm>
using namespace std;

int main()
{
	long long int n;
	scanf("%d", &n);
	long long int sum = 0;
	for (long long int i = 1; i < n; i++)
		sum += n*i + i;
	
	printf("%lld", sum);
	return 0;
}
