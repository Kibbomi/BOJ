#include <cstdio>
using namespace std;

int main()
{
	int t;
	scanf("%d", &t);

	while (t--)
	{
		int n;
		scanf("%d", &n);
		int ret = 0;
		for (int i = 0; i < n; ++i)
		{
			int temp;
			scanf("%d", &temp);
			ret += temp;
		}
		printf("%d\n", ret);
	}
	return 0;
}
