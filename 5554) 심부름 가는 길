#include <stdio.h>
#include <algorithm>
using namespace std;

int main(void)
{	
	int temp, sum = 0;
	for (int i = 0; i < 4; i++)
	{
		scanf("%d", &temp);
		sum += temp;
	}
	printf("%d\n", sum / 60);
	printf("%d", sum % 60);
	return 0;
}
