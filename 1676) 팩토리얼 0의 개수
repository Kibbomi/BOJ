#include <stdio.h>

int main(void)
{
	int n;
	scanf("%d", &n);
	int five = 0;
	for (int i = 1; i <= n; i++)
	{
		if (i % 5 == 0)
		{
			five++;
			if (i % 25 == 0)
				five++;
			if (i % 125 == 0)
				five++;
		}

	}
	printf("%d", five);
	return 0;
}
