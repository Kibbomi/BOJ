#include <stdio.h>

int main(void)
{
	int t;
	scanf("%d", &t);

	while (t--)
	{
		int ret = 0;
		int n, k;
		scanf("%d %d", &n, &k);

		for (int i = 0; i < n; i++)
		{
			int val;
			scanf("%d", &val);

			ret += val / k;
		}
		printf("%d\n", ret);
	}
	return 0;
}
