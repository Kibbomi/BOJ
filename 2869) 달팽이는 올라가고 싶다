#include <stdio.h>
using namespace std;

int main(void)
{
	int a, b, v;

	scanf("%d %d %d", &a, &b, &v);

	
	int d = 0;

	if( (v-b) % (a-b) == 0)
		d = (int)((v - b) / (a - b));
	else
		d= (int)((v - b) / (a - b)) +1 ;

	printf("%d", d);
	




	return 0;
}
