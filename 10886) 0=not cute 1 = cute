#include <stdio.h>

int main(void)
{
	int yes = 0 , no = 0;
	int n;
	scanf("%d", &n);
	for (int i = 0; i < n; i++)
	{
		int temp;
		scanf("%d", &temp);
		if (temp)
			yes++;
		else
			no++;
	}

	if (yes > no)
		printf("Junhee is cute!");
	else
		printf("Junhee is not cute!");
	return 0;
}
